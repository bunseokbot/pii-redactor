apiVersion: pii.namjun.kim/v1alpha1
kind: PIIAlertChannel
metadata:
  name: slack-alerts
  namespace: pii-system
spec:
  type: slack
  slack:
    webhookURL:
      name: slack-webhook
      key: url
    channel: "#security-alerts"
    username: "PII Redactor"
    iconEmoji: ":shield:"
  minSeverity: medium
  rateLimitPerMinute: 10
