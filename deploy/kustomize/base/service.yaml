apiVersion: v1
kind: Service
metadata:
  name: pii-redactor-metrics
  namespace: pii-system
  labels:
    app.kubernetes.io/name: pii-redactor
    app.kubernetes.io/component: controller
spec:
  ports:
    - port: 8080
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    app.kubernetes.io/name: pii-redactor
    app.kubernetes.io/component: controller
